---
phase: 01-foundation-capture-spine
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/App.tsx
  - src/components/BoothPreview.tsx
  - src/hooks/useUserMedia.ts
  - src/lib/cameraErrors.ts
  - src/index.css
autonomous: true
user_setup: []

must_haves:
  truths:
    - "After permission grant, user sees live camera video filling the preview region."
    - "Deny camera / insecure context / no camera / camera busy each shows a clear, non-empty error state with next-step text (not a blank screen)."
  artifacts:
    - "useUserMedia hook (or equivalent) encapsulating stream lifecycle"
    - "cameraErrors helper mapping DOMException names to UI copy"
  key_links:
    - "video element uses playsInline and autoplay/muted as needed for mobile"
---

<objective>
Implement **live camera preview** with **robust error surfaces** for `getUserMedia` (FND-01, CAP-01).

Purpose: Establishes the media spine before countdown and capture.

Output: Guest-visible preview + error UI; stream held in React state with proper cleanup on unmount.
</objective>

<context>
@Scribe/.planning/ROADMAP.md
@Scribe/REQUIREMENTS.md (FND-01, CAP-01)
@Scribe/.planning/phases/01-foundation-capture-spine/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: cameraErrors + secure context guard</name>
  <files>src/lib/cameraErrors.ts, src/App.tsx or BoothPreview.tsx</files>
  <action>Create `src/lib/cameraErrors.ts` exporting a function `getCameraUserMessage(error: unknown, isSecureContext: boolean): { title: string; detail: string }` covering at minimum: non-secure context (when `!window.isSecureContext`), `NotAllowedError`, `NotFoundError`, `NotReadableError`, `OverconstrainedError`, `SecurityError`, and unknown fallback. Copy should be short, booth-friendly, actionable (e.g. “Allow camera in the address bar”, “Plug in a camera”, “Try https or localhost”).</action>
  <verify>Typecheck passes; unit test optional—manual grep that all listed error names are handled.</verify>
  <done>Importer can render title+detail for any thrown error.</done>
</task>

<task type="auto">
  <name>Task 2: useUserMedia hook + BoothPreview</name>
  <files>src/hooks/useUserMedia.ts, src/components/BoothPreview.tsx, src/App.tsx, src/index.css</files>
  <action>Implement `useUserMedia` with `{ stream, error, status: idle|pending|live|error }`, calling `navigator.mediaDevices.getUserMedia({ video: { facingMode: 'user' }, audio: false })` (adjust constraints if needed for desktop). **Stop all tracks** on unmount. `BoothPreview` renders `<video ref={} autoPlay playsInline muted />` bound to stream; CSS: full-viewport or large centered 16:9 box with `object-fit: cover`; **mirror** with `transform: scaleX(-1)` on video wrapper for selfie UX. On error, show error UI using `getCameraUserMessage`. Wire into `App` replacing placeholder.</action>
  <verify>Manual: localhost dev shows live preview when allowed; deny permission shows message; `npm run build` passes.</verify>
  <done>Live preview + error states observable in browser.</done>
</task>

</tasks>

<verification>
Chrome or Safari: grant/deny permission paths tested once each.
</verification>

<success_criteria>
CAP-01 and FND-01 error clarity satisfied for preview step (no compositing yet).
</success_criteria>

<output>
After completion, create `01-02-SUMMARY.md` in this phase directory.
</output>

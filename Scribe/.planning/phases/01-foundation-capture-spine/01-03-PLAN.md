---
phase: 01-foundation-capture-spine
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/App.tsx
  - src/components/CountdownOverlay.tsx
  - src/hooks/useBoothState.ts
  - src/audio/cues.ts
  - src/index.css
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can start a flow that shows a visible countdown (e.g. 3-2-1) before capture."
    - "Optional audio cues play during countdown and at shutter moment unless user has toggled Mute."
    - "If prefers-reduced-motion: reduce or remove decorative motion on countdown (keep numbers readable)."
  artifacts:
    - "Central booth state machine: idle → preview → countdown → review (stub ok until 01-04) or minimal transition to capture-ready"
    - "Mute toggle persisted for session (sessionStorage or React state)"
  key_links:
    - "Audio uses Web Audio API or HTMLAudioElement; must not throw if autoplay blocked—catch and continue visual-only"
---

<objective>
Add **countdown / ready sequence**, **audio cues**, **mute control**, and a minimal **state machine** bridging preview to the next capture step (CAP-02, AUD-01).

Purpose: Booth pacing and accessibility baseline before still capture.

Output: Countdown overlay, cue playback, mute UI, `prefers-reduced-motion` handling for countdown animation.
</objective>

<context>
@Scribe/REQUIREMENTS.md (CAP-02, AUD-01)
@Scribe/.planning/research/PITFALLS.md (audio/autoplay)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Booth state machine</name>
  <files>src/hooks/useBoothState.ts, src/App.tsx</files>
  <action>Implement typed states: `preview | countdown | capturing | review` (capturing can be minimal until 01-04). Transitions: from preview, **Start** → countdown; countdown completes → `capturing` (or directly to capture trigger in 01-04—if so, countdown end fires callback `onCountdownComplete`). Export actions: `startCountdown`, `resetToPreview`. Prevent double-start while countdown active.</action>
  <verify>Typecheck; manual click-through preview → countdown → next state.</verify>
  <done>State transitions are explicit and logged only in dev if desired—no impossible states.</done>
</task>

<task type="auto">
  <name>Task 2: Countdown UI + reduced motion</name>
  <files>src/components/CountdownOverlay.tsx, src/index.css, src/App.tsx</files>
  <action>Overlay shows large numeric countdown (3,2,1) or configurable duration. Use `window.matchMedia('(prefers-reduced-motion: reduce)')` to **disable** pulse/scale animations; keep high-contrast numbers. Position overlay above video preview.</action>
  <verify>Toggle reduced motion in OS devtools; countdown remains legible with minimal animation.</verify>
  <done>CAP-02 visual sequence works; reduced motion respected.</done>
</task>

<task type="auto">
  <name>Task 3: Audio cues + mute</name>
  <files>src/audio/cues.ts, src/App.tsx</files>
  <action>Implement simple cues: tick or beep per second + distinct shutter sound at T-0. Prefer **Web Audio** (`AudioContext`, oscillator beeps) to avoid asset hosting, **or** ship small MP3/OGG in `public/sfx/` if you choose files—pick one approach and document in SUMMARY. **Mute** button in chrome sets internal `muted` flag so gain=0 or no `play()`. If `AudioContext` suspended (autoplay policy), show subtle hint “Tap for sound” on first interaction and resume on click. Never throw uncaught from audio layer.</action>
  <verify>With sound on: hear cues; mute: silent; build passes.</verify>
  <done>AUD-01 satisfied for countdown path.</done>
</task>

</tasks>

<verification>
Countdown completes reliably; mute persists across one countdown cycle.
</verification>

<success_criteria>
Guests experience timed cueing with optional audio and accessible motion defaults.
</success_criteria>

<output>
After completion, create `01-03-SUMMARY.md` in this phase directory.
</output>

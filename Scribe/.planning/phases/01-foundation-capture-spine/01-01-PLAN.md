---
phase: 01-foundation-capture-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - index.html
  - public/manifest.webmanifest
  - public/vite.svg
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/vite-env.d.ts
  - docs/DEV.md
  - README.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "`npm run dev` serves the app on localhost with hot reload."
    - "Visiting the app shows a minimal shell (title + placeholder) without console errors."
    - "README or docs/DEV.md states HTTPS/localhost requirement for camera in later plans."
  artifacts:
    - "package.json with Vite + React + TypeScript scripts"
    - "public/manifest.webmanifest with name, icons, display standalone or fullscreen, theme colors"
  key_links:
    - "index.html references manifest link rel=manifest"
---

<objective>
Bootstrap the **Scribe** web app with **Vite + React + TypeScript**, a valid **Web App Manifest**, and **developer notes** for secure-context / kiosk deployment.

Purpose: Unblocks all capture work; satisfies **FND-02** scaffolding and prepares **FND-01** messaging surface in later plans.

Output: Runnable dev tree under `Scribe/` (project root), manifest linked, docs for operators/devs.
</objective>

<execution_context>
Executor: follow repo patterns; keep changes inside `Scribe/` unless monorepo constraints require otherwise.
</execution_context>

<context>
@Scribe/.planning/ROADMAP.md (Phase 1)
@Scribe/REQUIREMENTS.md (FND-01, FND-02)
@Scribe/.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite React TS app</name>
  <files>package.json, package-lock.json, vite.config.ts, tsconfig*.json, index.html, src/main.tsx, src/App.tsx, src/index.css, src/vite-env.d.ts, public/vite.svg</files>
  <action>If `Scribe/package.json` is missing, run `npm create vite@latest . -- --template react-ts` in `Scribe/` (or equivalent) so the result is a standard Vite 6+/8.x React TS app. Add npm scripts `dev`, `build`, `preview`. Ensure `strict` TypeScript. Replace default marketing copy in `App.tsx` with a minimal **Scribe** placeholder (h1 + short subtitle). Remove unused demo assets if any beyond a single icon.</action>
  <verify>From `Scribe/`: `npm install` then `npm run build` exits 0.</verify>
  <done>Production build succeeds; dev server starts without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Web App Manifest + wire-up</name>
  <files>public/manifest.webmanifest, index.html, public/icons (if added)</files>
  <action>Create `public/manifest.webmanifest` with `name`, `short_name`, `start_url` (e.g. `/`), `display` set to `standalone` or `fullscreen`, `theme_color`, `background_color`, and at least one icon entry (192 and 512 if you add PNGs, or reuse a single SVG only if you also provide PNGs—**PWA install expects PNG** on most platforms: add simple generated or placeholder PNGs in `public/`). Add `<link rel="manifest" href="/manifest.webmanifest" />` in `index.html`.</action>
  <verify>Build output includes manifest in `dist/`; `curl` or file read shows valid JSON fields.</verify>
  <done>Manifest parses; linked from HTML; icons resolve in dev server paths.</done>
</task>

<task type="auto">
  <name>Task 3: DEV.md + README touch-up</name>
  <files>docs/DEV.md, README.md</files>
  <action>Add `docs/DEV.md` covering (1) `npm run dev` / `build` / `preview`, (2) **camera requires secure context** — use `https://localhost` or trusted HTTPS for real device tests; (3) optional note for Chrome kiosk launch (`--kiosk` URL) as operator concern. Update root `README.md` to point at DEV.md and one-line project purpose (booth Phase 1).</action>
  <verify>Files exist and are Markdown-valid.</verify>
  <done>New contributor can run dev and understand why HTTP LAN IP might block camera.</done>
</task>

</tasks>

<verification>
- `npm run build` passes in `Scribe/`.
- Manifest present and linked.
</verification>

<success_criteria>
Phase 1 plan 01 complete when the repo contains a buildable Vite React TS app with manifest and dev HTTPS guidance.
</success_criteria>

<output>
After completion, create `Scribe/.planning/phases/01-foundation-capture-spine/01-01-SUMMARY.md` per execute workflow.
</output>

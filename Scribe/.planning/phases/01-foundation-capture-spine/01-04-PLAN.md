---
phase: 01-foundation-capture-spine
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/App.tsx
  - src/components/ReviewScreen.tsx
  - src/lib/captureStill.ts
  - src/index.css
autonomous: true
user_setup: []

must_haves:
  truths:
    - "After countdown, app grabs a still frame from the live video into a bitmap the user can see on a review screen."
    - "User can Retake (return to preview + new countdown) or Accept/Download."
    - "User always gets a file download (PNG or JPEG) of the accepted still; filename includes scribe/timestamp."
    - "If Web Share Level 2 with files is available and allowed, offer Share button; if share fails or unavailable, download still works."
  artifacts:
    - "captureStill.ts drawing video frame to canvas and returning Blob"
    - "ReviewScreen showing captured image + Retake + Download (+ Share when supported)"
  key_links:
    - "canvas is origin-clean (camera-only); toBlob wrapped with error UI if it ever fails"
---

<objective>
Complete the **capture → review → retake → export** loop: still frame from video, **retake**, **download**, **Web Share with fallback** (CAP-03, CAP-04, EXP-01, EXP-02).

Purpose: Delivers Phase 1 success criteria end-to-end without compositing.

Output: Working booth MVP slice: preview → countdown → still → review → download/share.
</objective>

<context>
@Scribe/REQUIREMENTS.md (CAP-03, CAP-04, EXP-01, EXP-02)
@Scribe/.planning/research/FEATURES.md (Web Share)
</context>

<tasks>

<task type="auto">
  <name>Task 1: captureStill utility</name>
  <files>src/lib/captureStill.ts</files>
  <action>Export `captureStillFromVideo(video: HTMLVideoElement, options?: { type?: string; quality?: number }): Promise<Blob>` that creates an offscreen or onscreen `canvas` sized to `video.videoWidth`/`video.videoHeight` (guard for 0—reject with clear error), `drawImage(video,0,0)`, returns `new Promise` from `canvas.toBlob`. Default to `image/jpeg` with reasonable quality for file size. No cross-origin images on canvas.</action>
  <verify>Call from dev stub or test with mock canvas if needed; `npm run build` passes.</verify>
  <done>Blob resolves when video has dimensions and frame drawn.</done>
</task>

<task type="auto">
  <name>Task 2: ReviewScreen + retake loop</name>
  <files>src/components/ReviewScreen.tsx, src/hooks/useBoothState.ts, src/App.tsx</files>
  <action>On countdown complete, capture still (async), transition to `review`, show `ReviewScreen` with `object-fit: contain` image. **Retake** stops showing still, returns to `preview` (keep stream running), clears captured blob from state. **Accept** triggers download path. Handle capture errors (show toast or inline error + return to preview).</action>
  <verify>Retake returns to live preview; second countdown works.</verify>
  <done>CAP-04 retake path works.</done>
</task>

<task type="auto">
  <name>Task 3: Download + Web Share fallback</name>
  <files>src/lib/shareDownload.ts, src/App.tsx or ReviewScreen.tsx</files>
  <action>Implement `downloadBlob(blob, filename)` via `URL.createObjectURL` + `<a download>` click + `revokeObjectURL`. Implement `shareBlob(blob, filename)` calling `navigator.canShare?.({ files })` then `navigator.share` with `File` from blob; on any failure or unsupported API, return false so UI shows only Download. **Primary button:** “Save photo” always visible; **secondary:** “Share” only when `canShare` true. User gesture required for share.</action>
  <verify>Desktop: download works; mobile Safari/Chrome: test share when available, else download only without error.</verify>
  <done>EXP-01 and EXP-02 satisfied.</done>
</task>

</tasks>

<verification>
Full manual path: preview → countdown → review → download; retake → repeat; share tested on one mobile browser if accessible.
</verification>

<success_criteria>
All Phase 1 roadmap success criteria achievable in manual test on HTTPS or localhost.
</success_criteria>

<output>
After completion, create `01-04-SUMMARY.md` in this phase directory.
</output>
